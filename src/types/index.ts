export type Product = {
  id: string;

  name: string;
  imageURL: string;
  slug: string;
  image: string;
  price: number;
  description: string;

  createdAt: Date;
  updatedAt: Date;
};
